// We can define the database url 
// in the settings tab of the
// Heroku interface, get the 
// url through the Heroku CLI,
// and create a variable in the
// Heroku interface which determines
// whether there's a database in
// Heroku.  

// We can use grep to 
// find text in the terminal,
// after we enter the word history
// in the terminal, then run 
//!WhateverNumberCorresponds-
// withTheCommandLine command
// we want to run again.