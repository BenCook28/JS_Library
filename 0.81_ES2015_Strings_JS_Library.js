// Template Strings
// Instead of first + " " + last;,
// we do `${first} ${last}`;

// We can also write multi-line
// strings with template strings.
// for example:

let message = `Hi ${user},

this is 

a 

message.

Sincerely,
${admin.fullname}`;