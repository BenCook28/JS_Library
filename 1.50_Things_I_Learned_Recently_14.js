// Axios doesn't work with 
// Android, but works with
// iOS.  We need to put
// gradle variables in
// strings.  We archive
// Xcode builds through the
// bar that appears when
// we hover near the top
// of the screen.  With
// .jsx files, we get
// formatting suggestions
// which can reduce errors.
// Instead of Axios, we
// can use fetch with 
// Android code to access
// APIs.