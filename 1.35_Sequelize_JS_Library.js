// With Sequelize, we can 
// use the same code
// related to getting data into
// SQL databases and get the code 
// into Postgres, MySQL, Microsoft
// SQL, Oracle SQL, or any other kind

// We specify the dialect of SQL in
// the models folder, and Sequelize
// does the rest.  

// Thanks to Sequelize, users don't
// have to re-make database schemas
// when they clone code.