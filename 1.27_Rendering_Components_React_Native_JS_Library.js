// A helper method is method that
// helps another method perform its'
// task.

// The .map() function is an ES2015 
// function for iterating over arrays.

// .map() takes a fat arrow function as 
// a parameter.  It looks like this:

// renderAlbums() {
// 	this.state.albums.map(album => <Text>{album.title}</Text>)
// }

// album is one item in the array.