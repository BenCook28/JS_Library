// React Native buttons use onTextChange,
// while React buttons use onChange.

// React functions need to be scoped like
// exampleFunction = (param) => {
	//code;
// }

// While React Native functions
// can look like exampleFunctionRN(param) {
	//code;
// }