//Typeof returns either 'number', 'string', 'boolean', 'undefined', 'function', or 'object'.

//If you use typeof on an array or null, the result will incorrectly be 'object.'

//! reverses the truthiness of an operand.

//+ adds if both operands are numbers and otherwise concatenates.

// / can return a noninteger result even if bother operands are integers.

// The && operator produces the value of its first operand if the first operand is falsy. 
//Otherwise it produces the value of the second operand.

//The paragraph on && goes against my experience, so I'll get clarification
//tomorrow.