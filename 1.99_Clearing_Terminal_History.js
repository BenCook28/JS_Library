// Entering clear into the terminal
// only clears the current log values,
// but doesen't clear the history.
// So previous logs still appear when
// we run the code again.
// clear && printf '\e[3J' clears the 
// terminal history too.