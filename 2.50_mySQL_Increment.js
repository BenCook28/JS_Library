// If a PHP email function doesn't work
// because it says a record which doesn't
// esist is a duplicate, we can run a 
// query to increase the increment by one.
// This skips the "duplicate" record.