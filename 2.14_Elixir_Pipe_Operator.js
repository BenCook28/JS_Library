// |> solves the problem that
// nested functions can be 
// hard to read at times.

// The pipe operator takes the
// output of the expression on
// the left and passes it as the
// first argument of the expression
// on the right.