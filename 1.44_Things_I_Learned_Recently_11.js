// If we install a new node modules
// and Node or React Native doesn't
// recognize it, we can delete
// node modules and then run
// npm install.

// Stateless components are for
// presenting data only,
// and require less code
// writing and improve
// performance.

// Because stateless components
// are functions and not
// classes, they have no this.