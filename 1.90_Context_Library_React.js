// The Context library in React
// makes it easy to pass props
// deep in component trees,
// but it can cause issues if 
// overused.  In general, it makes
// sense to use Context if all
// the components will use the
// prop, as with styling themes.
// Otherwise, a flux pattern 
// like Redux or MobXwould work 
// better.