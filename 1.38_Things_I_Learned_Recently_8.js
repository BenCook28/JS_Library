// We don't put const or let
// before variables in React
// classes, but we do
// put const and let in front
// of variables within functions.

// ES6 concatination looks like
// this:  `text${JS}more_text`

// For form validation, !username
// covers both undefined and null.

// We can use React Navigator to go 
// back more than one view.

// We can use sequelize command line
// tools to create new tables and 
// migrate them to the database.