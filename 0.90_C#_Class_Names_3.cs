using Band;
public class Band // makes the code accessible anywhere.
// private makes code available in the same class only.
// We can make variables and functions public too.

Band band = new Band();
// creates a new instance of the Band class.