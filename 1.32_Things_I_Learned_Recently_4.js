// Whenever I don't know what's wrong
// with a React Native application,
// I can kill the packager and restart it.

// git doesn't handle capitalization changes
// well. The convention is to use lowercase
// with the components folder.

// We can use Viro Media for VR and AR with
// React Native.

// We don't need to pass props through a 
// function because React Native already
// has access to them.