// If I run react-native run-ios,
// then I don't have to run nodemon 
// index.js, gulp, or any other
// method I've used to run apps locally.

// I need to install ESLint on every
// project in order to use it, even
// though I installed part of it globally.

// Mixins aren't used anymore in React Native
// for calling functions at a later time.

// On a MacBook Pro CMD Left arrow works the
// same as the home key and CMD right arrow
// works the same as the end key.