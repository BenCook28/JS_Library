// We need to put $ signs below every instance of 
// the iterating variable in PHP as shown below:

// for ( $x = 0; $x < count($values); $x++) {
//     if(strcmp($values[$x], "in_office") == 0) {
//     // if($source == "on") {
//         $container->add_child($text);  
//     }
// }