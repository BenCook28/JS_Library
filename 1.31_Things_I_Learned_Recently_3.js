// I made a mistake in my entry from 
// yesterday, with Axios posts the
// correct order is link, data, headers.

// Pinching with four fingers on a 
// Mac shows all open apps.

// We access the value associated
// with an object key with Object[key].

// The Object && Object syntax works in
// a similar way as the terinary operator.
// it says, "does something exist or is it true?

// We can create package.json files within a 
// subfolder so shorten the paths to files.