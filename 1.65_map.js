// maps in ES6 take an array
// and return a new array 
// as an output, thus not
// mutating the state.

// In the render function, 
// we can iterate over the
// new array with something
// like 

// this.props.transactions.map(renderTransaction).

// after putting key={i} in the 
// renderTransactions function.
// renderTransactions could have
// any name.