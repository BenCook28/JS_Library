// In React Native,
// we can conditionally render
// below the render() function
// by first initializing variables,
// and in the logic following if statements,
// assigning values to the variables
// we declared.