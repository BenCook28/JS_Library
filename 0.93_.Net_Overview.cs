// views are in cshtml format.
// Using the same name across files
// synces MVC.

// @model String says our view expects
// a single string of data.

// To access model data in our view,
// we use @Model.

// IActionResult gets returned in
// action methods, which provide 
// responses usable to browsers.

// home/index is typically the default route.