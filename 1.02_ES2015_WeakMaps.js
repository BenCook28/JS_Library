// Only objects can be passed in as keys in 
// WeakMaps.  We can use .get(key)
// .has(key), and .delete(key) methods in weak maps.

// WeakMaps are not iterable.
// WeakMaps are better for memory.
// The objects referenced can be garbage collected
// when no longer used.