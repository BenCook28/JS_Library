// When we use uncontrolled components,
// we use ref properties to directly access
// the DOM.  

// The ref prop is a function with the DOM 
// element as its argument.

// React recommends using controlled components
// so we don't interact directly with the DOM.