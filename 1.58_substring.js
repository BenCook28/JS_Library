// substring returns a shorter string,
// for example, we might have a long
// date with a timestamp and time zone
// stored in the variable longDate.
// If we only want a date like 2017-12-06,
// we could run longDate.substring(0, 10);