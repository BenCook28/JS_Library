// We run the Anroid emulator
// from the tools folder.
// Because the phone 
// simulator has a
// different IP address,
// we set an IP address where
// we usually declare localhost
// in the React Native code
// to fix network errors.