// We don't need to import Reducers.
// In redux connect statements, the
// mapStateToProps function goes first,
// followed by an optional second
// argument which covers what we'll
// pass into the store if anything,
// and the third argument is the name
// of the module we're exporting.

// Also, this sets upsteam:
// git push -u origin my_branch

//this is to test whether -u worked